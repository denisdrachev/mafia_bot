spring:
  datasource:
    url: jdbc:h2:file:./data/mafia;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

logging:
  level:
    com.example.mafia: DEBUG
